# Consent Modules Restructure Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Restructure Module 4 (Consent Fundamentals) and Module 5 (Consent Configuration) for better pedagogical flow, and introduce a Project page stub for the Consent Automation Flow with sidebar prefix treatment.

**Architecture:** All changes are content-only (markdown rewrites and section reorders) except the sidebar spike, which requires a doc-level `customProps` frontmatter field on the Project page stub and a CSS/swizzle investigation. The Project page stub lives at `docs/part-1-foundations/consent-automation-flow.md` with `sidebar_position: 6` (between Module 5 and future modules).

**Tech Stack:** Docusaurus 3.10, autogenerated sidebar (`sidebars.ts`), `_category_.json` for directory-level sidebar config, `customProps` frontmatter for per-doc sidebar metadata.

## Global Constraints

- No em dashes, exclamation marks, semicolons, or ellipses in prose
- No banned words (see `.planning/WRITING-STYLE-GUIDE.md` — leverage, utilize, robust, etc.)
- No banned admonitions: `:::note`, `:::danger` only — use `:::info`, `:::warning`, `:::tip`, `:::caution`
- Terminology: "Data 360" not "Data Cloud"; "MCA" not "Marketing Cloud Growth"
- All `<!-- VERIFY -->` comments must be preserved exactly as-is — do not remove or rewrite them
- Section order in every module: Overview → Lesson overview → Lesson body (freeform H2s) → Assignment → Success Criteria → Knowledge check → Additional resources
- Lint check after every module edit: `npm run lint:content docs/part-1-foundations/<filename>.md`
- The consent automation flow content itself is TBD — the Project stub gets frontmatter + a placeholder body, not new lesson content

---

### Task 1: Restructure Module 4 section order + add diagram reference

**Files:**
- Modify: `docs/part-1-foundations/consent-fundamentals.md`

**Interfaces:**
- Consumes: nothing (standalone content reorder)
- Produces: Module 4 with revised section order matching the spec's "Revised section order" list

The target section order is:
1. Overview
2. Lesson overview
3. Explicit Opt-In, Not Opt-Out
4. The Five Consent Components *(with external diagram reference paragraph added at top)*
5. Global Opt-Out vs. Subscription-Level Opt-Out *(moved from position 7 of 12)*
6. How Consent is Enforced at Send Time
7. The Consent Audit Trail *(moved out of component list, brief subsection after enforcement)*
8. The 90-Day Consent Cache
9. Valid Methods for Creating Consent Records
10. The Party Field Gotcha *(moved from position 11 of 12)*
11. The Preference Center
12. Double Opt-In
13. Legal Compliance Context
14. V1/V2 DMO Migration
15. LEOptical's Four Communication Subscriptions
16. Assignment
17. Success Criteria
18. Knowledge check
19. Additional resources

Changes required:
- **Move** "Global Opt-Out vs. Subscription-Level Opt-Out" from after "Double Opt-In" to immediately after "The Five Consent Components" section
- **Move** "The Party Field Gotcha" from after "V1/V2 DMO Migration" to immediately after "Valid Methods for Creating Consent Records"
- **Move** "Consent Audit Trail (DLO)" subsection (`### Consent Audit Trail (DLO)`) out of the `## The Five Consent Components` section and into a standalone `## The Consent Audit Trail` section placed immediately after `## How Consent is Enforced at Send Time`
- **Add** a diagram reference paragraph at the top of `## The Five Consent Components` (before the first `### Contact Point` heading):

```
Before reading the sections below, look at [The Agentic Marketer's consent management diagram](https://the-agentic-marketer.com/marketing-cloud-next-deep-dives/consent-management/). Their diagram shows how these five objects connect to each other. The prose below explains what each one does and where the gotchas are.
```

- **Update** the Lesson overview bullets to reflect that Consent Audit Trail is now a standalone section (not listed as part of the five components)

The Lesson overview bullet currently reads:
```
- The Consent Audit Trail DLO and why it is not used at send time.
```
Keep this bullet — it describes a topic, not a position. No bullet changes needed beyond verifying they still match the new section order.

- [ ] **Step 1: Rewrite the file with sections in the new order**

Open `docs/part-1-foundations/consent-fundamentals.md`. Copy the file content. Reconstruct the file with sections in this exact order, cutting and pasting each section verbatim (no prose changes). The only new prose is the diagram reference paragraph added at the top of `## The Five Consent Components`.

Verify the Consent Audit Trail moves correctly:
- Remove `### Consent Audit Trail (DLO)` from inside `## The Five Consent Components`
- Add it as a new `## The Consent Audit Trail` section immediately after `## How Consent is Enforced at Send Time`, with this framing sentence prepended to the existing prose:

```
The Consent Audit Trail is a logging mechanism, not part of the enforcement chain described above.
```

Then the existing paragraph follows unchanged.

- [ ] **Step 2: Run the linter**

```bash
npm run lint:content docs/part-1-foundations/consent-fundamentals.md
```

Expected: no errors. Warnings on `<!-- VERIFY -->` comments are acceptable.

- [ ] **Step 3: Commit**

```bash
git add docs/part-1-foundations/consent-fundamentals.md
git commit -m "refactor: restructure Module 4 section order; add consent diagram reference"
```

---

### Task 2: Update Module 5 frontmatter description

**Files:**
- Modify: `docs/part-1-foundations/consent-configuration.md`

**Interfaces:**
- Consumes: nothing
- Produces: updated `description` frontmatter field that no longer references the triggered flow

Current frontmatter `description`:
```
Configure Communication Subscriptions, build a triggered flow to auto-create consent records, and add the Privacy Consent Status component to CRM record pages.
```

New frontmatter `description`:
```
Configure Communication Subscriptions, set up the preference page and web tracking consent banner, and add the Privacy Consent Status component to CRM record pages.
```

- [ ] **Step 1: Update the description field in frontmatter**

Edit line 4 of `docs/part-1-foundations/consent-configuration.md`. Change only the `description` value. Touch nothing else in this step.

- [ ] **Step 2: Run the linter**

```bash
npm run lint:content docs/part-1-foundations/consent-configuration.md
```

Expected: no errors.

- [ ] **Step 3: Commit**

```bash
git add docs/part-1-foundations/consent-configuration.md
git commit -m "fix: remove flow reference from Module 5 frontmatter description"
```

---

### Task 3: Restructure Module 5 lesson body section order

**Files:**
- Modify: `docs/part-1-foundations/consent-configuration.md`

**Interfaces:**
- Consumes: Task 2 output (updated frontmatter already committed)
- Produces: Module 5 lesson body with sections in the spec's proposed order

Target lesson body section order:
1. Communication Subscriptions *(creation walkthrough — existing content)*
2. The default preference page *(existing content)*
3. Privacy Consent Status component *(moved — currently appears after "Building the consent automation flow")*
4. CSV consent import *(moved — currently appears after Privacy Consent Status)*
5. The Create Consent Request flow element *(reference table — moved from before "Building the consent automation flow" to here)*
6. Web tracking consent banner *(moved to end of lesson body)*
7. Org-wide consent settings *(existing, stays last)*

Currently the sections appear in this order in the file:
1. Communication Subscriptions
2. The default preference page
3. Web tracking consent banner
4. The Create Consent Request flow element
5. Building the consent automation flow *(TBD placeholder — being extracted to Project page)*
6. Privacy Consent Status component
7. CSV consent import
8. Org-wide consent settings

The `## Building the consent automation flow` section and its `:::caution` admonition and closing paragraph are **removed from this file** (they move to the Project page stub in Task 5). Do not delete the section's content — you will paste it into the Project page in Task 5. Copy it before removing.

- [ ] **Step 1: Restructure the lesson body by cutting and pasting sections into the target order**

Cut each `##` section verbatim and paste in this order:
1. `## Communication Subscriptions` (unchanged position)
2. `## The default preference page` (unchanged position)
3. `## Privacy Consent Status component` (move up from position 6)
4. `## CSV consent import` (move up from position 7)
5. `## The Create Consent Request flow element` (move from position 4)
6. `## Web tracking consent banner` (move from position 3)
7. `## Org-wide consent settings` (unchanged position)

Remove `## Building the consent automation flow` and its full content (the TBD comment block, the `:::caution` admonition, and the closing paragraph + link).

- [ ] **Step 2: Run the linter**

```bash
npm run lint:content docs/part-1-foundations/consent-configuration.md
```

Expected: no errors.

- [ ] **Step 3: Commit**

```bash
git add docs/part-1-foundations/consent-configuration.md
git commit -m "refactor: reorder Module 5 lesson body sections; extract flow section to project page"
```

---

### Task 4: Add transition sentence + update Module 5 overview

**Files:**
- Modify: `docs/part-1-foundations/consent-configuration.md`

**Interfaces:**
- Consumes: Task 3 output (reordered sections)
- Produces: Module 5 with (a) a transition sentence in Communication Subscriptions, (b) an updated overview that references the Project page

**Change A — Transition sentence:**

Find the opening of `## Communication Subscriptions`. It currently reads:

```
LEOptical's four Communication Subscriptions are defined in the Consent Fundamentals module. This section covers how to create them in the platform.
```

Insert a new sentence after the second sentence so the paragraph becomes:

```
LEOptical's four Communication Subscriptions are defined in the Consent Fundamentals module. This section covers how to create them in the platform. Communication Subscriptions are created and managed in the MCA Consent tab, separate from Salesforce Setup.
```

**Change B — Overview paragraph update:**

The current third paragraph in `## Overview` reads:

```
The consent automation flow (a Data 360-Triggered Flow that creates records for new individuals automatically) is covered in this module's flow section, but the walkthrough is pending a validated POC. For now, CSV import is the method you will use to get protagonist contacts into an OPT_IN state so you can continue with later modules.
```

Replace it with:

```
The consent automation flow is covered in a separate Project page. That page walks through the trigger mechanism and full flow architecture once the POC is validated. This module covers everything else: Communication Subscriptions, the preference page, the Privacy Consent Status component, CSV import, and the web tracking consent banner.
```

- [ ] **Step 1: Add the transition sentence to Communication Subscriptions**

Edit the second sentence of the `## Communication Subscriptions` opening paragraph. Add the transition sentence as written above. Do not change anything else.

- [ ] **Step 2: Update the overview paragraph**

Replace the third paragraph in `## Overview` with the new paragraph as written above.

- [ ] **Step 3: Update the Lesson overview bullets**

The current Lesson overview does not mention the flow at all — confirm this is true. If the bullets currently reference "the consent automation flow" or "the triggered flow," remove that bullet. Add a bullet that reads:

```
- Where the consent automation flow lives and why it is a separate Project page rather than a section in this module.
```

If no flow-related bullet currently exists, skip this step.

- [ ] **Step 4: Run the linter**

```bash
npm run lint:content docs/part-1-foundations/consent-configuration.md
```

Expected: no errors.

- [ ] **Step 5: Commit**

```bash
git add docs/part-1-foundations/consent-configuration.md
git commit -m "feat: add Module 5 transition sentence and update overview to reference Project page"
```

---

### Task 5: Create the Consent Automation Flow Project page stub

**Files:**
- Create: `docs/part-1-foundations/consent-automation-flow.md`

**Interfaces:**
- Consumes: the `## Building the consent automation flow` content removed from Task 3 (TBD placeholder, `:::caution` admonition, closing paragraph)
- Produces: a standalone Project page stub with correct frontmatter, minimal body, and the extracted TBD placeholder

The Project page stub follows the standard module template format (Overview → Lesson overview → lesson body → Assignment → Success Criteria → Knowledge check). Because the flow content is TBD, most sections are placeholder stubs.

The `sidebar_position` for this page is `6` — placing it after Module 5 (position 5) and before any future modules in Part 1.

Full file content:

```markdown
---
sidebar_position: 6
title: "Project: Consent Automation Flow"
description: "Build the Data 360-Triggered Flow that automatically creates consent records for new individuals in LEOptical's org."
custom_edit_url: null
---

## Overview

LEOptical's consent strategy relies on a flow that creates OPT_IN records automatically when a new individual enters the system. Without it, every new contact starts as blocked from marketing sends. Module 5 covers CSV import as a manual workaround. This project is the permanent solution.

The consent automation flow is a Data 360-Triggered Flow that fires on the Individual DMO. When a new Individual record is created, the flow writes OPT_IN records for each of LEOptical's three marketing Communication Subscriptions using the Create Consent Request flow element.

This walkthrough is pending a validated proof of concept in a live SDO. The trigger mechanism, entry conditions, and field mapping for the Create Consent Request element must be confirmed against actual platform behavior before this page can be completed. Do not attempt to build this flow from the Create Consent Request element reference table in Module 5 alone — the triggering mechanism has not yet been validated.

## Lesson overview

This section contains a general overview of topics that you will learn in this lesson.

- What a Data 360-Triggered Flow is and when it fires relative to record creation.
- How to configure the flow trigger on the Individual DMO.
- How to wire the Create Consent Request element for each of LEOptical's three marketing subscriptions.
- How to test the flow and verify that consent records appear on protagonist contacts.

## Walkthrough

{/* TBD: This walkthrough requires a validated POC before it can be documented. The correct trigger type is a Data 360-Triggered Flow on the Individual DMO. The steps will be written after testing the flow design in a live SDO to confirm trigger behavior, latency, and the correct field mapping for the Create Consent Request element. Do not build this flow until the POC is validated. */}

:::caution
This walkthrough is not yet available. The consent automation flow design requires hands-on validation in a live SDO before it can be documented accurately. For now, use the CSV import method in Module 5 to create consent records for your protagonist contacts so you can continue with later modules.
:::

For background on the bidirectional consent sync pattern (flow changes in MCA writing back to CRM, and vice versa), see the [modrzejewski.it consent sync guide](https://modrzejewski.it/blog/how-to-keep-consent-in-sync-between-salesforce-data-360-and-marketing-cloud-next/).

## Assignment

> **The client wants:** New contacts who enter the system to receive OPT_IN records automatically, without manual CSV import.

This assignment will be written when the walkthrough is complete. You should complete Module 5's CSV import step first so that your protagonist contacts have consent records while this flow is being validated.

## Success Criteria

- [ ] The Data 360-Triggered Flow is active and firing on new Individual records.
- [ ] Three Create Consent Request elements are configured — one per marketing subscription.
- [ ] A test contact created after the flow was activated shows OPT_IN for all three marketing subscriptions in the Privacy Consent Status component.

## Knowledge check

The following questions are an opportunity to reflect on key topics in this lesson. If you can't answer a question, revisit the relevant section, but keep in mind you are not expected to memorize or master this knowledge.

- What is the difference between a Data 360-Triggered Flow and a record-triggered Flow in Salesforce CRM?
- Why does the Create Consent Request element update the consent cache when a standard Create Records element writing to the same DMO does not?
- If a new Individual is created but the flow fails silently, what is the observable symptom in later modules?
- Why does LEOptical need three separate Create Consent Request elements in this flow rather than one?
```

- [ ] **Step 1: Create the file**

Write the full file content above to `docs/part-1-foundations/consent-automation-flow.md` exactly as written. Do not alter prose.

- [ ] **Step 2: Run the linter**

```bash
npm run lint:content docs/part-1-foundations/consent-automation-flow.md
```

Expected: no errors. The `<!-- VERIFY -->` style comment inside `{/* TBD */}` is an MDX comment and will not trigger the linter's VERIFY warning (it uses `{/* */}` not `<!-- -->`). If it does flag, add a `<!-- VERIFY -->` comment at the appropriate place per linter rules.

- [ ] **Step 3: Commit**

```bash
git add docs/part-1-foundations/consent-automation-flow.md
git commit -m "feat: add Consent Automation Flow project page stub"
```

---

### Task 6: Spike — Docusaurus sidebar label prefix for Project pages

**Files:**
- No files modified (this is a research spike)
- May produce: a decision recorded in `.planning/platform-gotchas.md`

**Goal:** Determine whether Project pages can be given a visual prefix or icon in the autogenerated sidebar without switching to a manual sidebar config. Answer three questions:

1. Does Docusaurus autogenerated sidebar support `customProps` on individual docs via frontmatter?
2. Is there a sidebar item label override available via frontmatter (`sidebar_label`)?
3. Can a Unicode symbol (e.g., `⚙` or `▶`) be prepended to the sidebar label via `sidebar_label` frontmatter without breaking anything?

**Research method:** Check Docusaurus 3.x docs for `sidebar_label` frontmatter support and `customProps`. The `sidebar_label` frontmatter field is the simplest path — if it works, we can set `sidebar_label: "⚙ Consent Automation Flow"` on the Project page doc without touching sidebar config.

- [ ] **Step 1: Check if `sidebar_label` frontmatter is supported in Docusaurus 3.10**

Fetch the Docusaurus 3.x sidebar metadata docs using context7 or check `docusaurus.config.ts` for any relevant sidebar config. The key question: does Docusaurus 3.x support `sidebar_label` as a frontmatter key on individual docs?

If yes: `sidebar_label` is the mechanism. A Unicode prefix on the label value is sufficient.

If no: investigate `customProps` support in autogenerated sidebars. Per Docusaurus docs, `customProps` in a doc's frontmatter does not affect sidebar label. It would require switching to explicit sidebar config, which is a bigger change.

- [ ] **Step 2: Test a Unicode prefix on the Consent Automation Flow page**

If `sidebar_label` is supported, add the following to `docs/part-1-foundations/consent-automation-flow.md` frontmatter:

```yaml
sidebar_label: "▶ Consent Automation Flow"
```

Run the dev server (`npm start`) and verify the label renders correctly in the sidebar without breaking the autogenerated structure.

- [ ] **Step 3: Record the findings in `.planning/platform-gotchas.md`**

Add a dated entry:

```markdown
## Sidebar label prefix for Project pages (2026-08-07)

`sidebar_label` frontmatter is supported in Docusaurus 3.x autogenerated sidebars. A Unicode prefix (e.g., `▶`) can be prepended to the label value to visually distinguish Project pages from lesson modules. No manual sidebar config required.

**Usage:** Add `sidebar_label: "▶ {Page Title}"` to any Project page's frontmatter.

OR (if not supported):

Docusaurus 3.x autogenerated sidebars do not support label customization per-doc via frontmatter beyond `sidebar_label`. `customProps` in frontmatter does not affect sidebar rendering. To add a visual prefix, the sidebar must be switched from autogenerated to explicit config in `sidebars.ts`. Deferred — use a naming convention in `title` frontmatter for now.
```

- [ ] **Step 4: Commit**

```bash
git add docs/part-1-foundations/consent-automation-flow.md .planning/platform-gotchas.md
git commit -m "chore: spike sidebar label prefix for Project pages; apply result to consent automation flow stub"
```

---

### Task 7: Apply sidebar treatment to the Consent Automation Flow page

**Files:**
- Modify: `docs/part-1-foundations/consent-automation-flow.md`

**Interfaces:**
- Consumes: Task 6 spike result — the confirmed mechanism for sidebar label prefix

**Note:** If Task 6 determined that `sidebar_label` with a Unicode prefix works, this task applies that to the consent automation flow page. If the spike determined the mechanism is not feasible without explicit sidebar config, this task records the decision and defers the implementation.

- [ ] **Step 1: Apply the sidebar label prefix (if feasible)**

If `sidebar_label` frontmatter works: add `sidebar_label: "▶ Consent Automation Flow"` to the frontmatter of `docs/part-1-foundations/consent-automation-flow.md`.

If not feasible: add a code comment to the frontmatter:

```yaml
# sidebar_label prefix deferred — requires explicit sidebar config, tracked in platform-gotchas.md
```

- [ ] **Step 2: Verify sidebar renders correctly**

Run `npm start`. Open the sidebar. Confirm the Project page label is visually distinguishable from adjacent lesson modules.

- [ ] **Step 3: Commit**

```bash
git add docs/part-1-foundations/consent-automation-flow.md
git commit -m "feat: apply sidebar label prefix to Consent Automation Flow project page"
```

---

## Self-Review

**Spec coverage check:**

| Spec requirement | Task |
|---|---|
| Add external diagram reference at top of Five Consent Components | Task 1 |
| Move Global Opt-Out section earlier (after Five Components) | Task 1 |
| Move Party Field Gotcha earlier (after Valid Methods) | Task 1 |
| Move Consent Audit Trail out of component list | Task 1 |
| Extract consent automation flow into Project page | Tasks 3 + 5 |
| Update Module 5 frontmatter description | Task 2 |
| Update Module 5 overview to reference Project page | Task 4 |
| Reorder Module 5 lesson body sections | Task 3 |
| Add transition sentence in Communication Subscriptions | Task 4 |
| Create Consent Automation Flow Project page stub | Task 5 |
| Spike Docusaurus sidebar icon/prefix | Task 6 |
| Apply sidebar treatment to Project page | Task 7 |

All seven implementation order items from the spec are covered. No gaps identified.

**Placeholder scan:** No TBD, TODO, or "implement later" in any plan task. Code blocks are provided for all prose changes. All section names are verbatim from the existing files.

**Type consistency:** No types — content-only changes. Section heading names are consistent between where they are cut and where they are pasted.
